[project]
name = "l4zyg33k-cheatsheets"
version = "0.1.0"
description = "Linux and infrastructure cheatsheets built with MkDocs"
requires-python = ">=3.12"
dependencies = [
  "mkdocs-material==9.6.20",
]

[tool.uv]
package = false
