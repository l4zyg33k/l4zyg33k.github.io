# 태그 탐색

다음 네 패싯을 조합해 관련 치트시트를 탐색합니다.

- `platform/…`: 실행 플랫폼
- `domain/…`: 기술 영역과 세부 영역
- `task/…`: 수행하려는 작업
- `component/…`: 핵심 도구 또는 구성 요소

각 태그의 `/`는 상위에서 하위로 내려가는 제어 어휘를 뜻합니다. 예를 들어 `domain/hardware/display`는 하드웨어 영역의 디스플레이 주제입니다.
